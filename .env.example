# Copy to .env and fill in your Supabase project values.
# Expo reads EXPO_PUBLIC_* vars at build time (safe to bundle in client).

EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Edge function env vars — set these in the Supabase dashboard:
# Settings → Edge Functions → Secrets
#
# SUPABASE_SERVICE_ROLE_KEY  (set automatically by Supabase runtime)
# UPCITEMDB_KEY=your-upcitemdb-api-key   (optional — free tier otherwise)
